

<%= form_for(resource, :as => resource_name, html: {class: "form-signin"}, :url => session_path(resource_name)) do |f| %>
	<h2 class="form-signin-heading">请登陆</h2>
  
	
	<%#= f.label :email %>
	<%= f.email_field :email, :autofocus => true, class: "form-control", placeholder: "请输入邮箱地址" %>

	<%#= f.label :password %>
	<%= f.password_field :password, class: "form-control", placeholder: "请输入密码"%>

	<% if devise_mapping.rememberable? -%>
		<%= f.check_box :remember_me %> <%= f.label :remember_me %>
	<% end -%>

	<%= f.submit "登 陆" , class: "btn btn-lg btn-primary btn-block" %>

	<%= render "devise/shared/links" %>

<% end %>




